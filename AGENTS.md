# AGENTS.md

Codex를 비롯한 코딩 에이전트는 이 저장소에서 아래 기준을 따릅니다.

## 목적과 우선순위

이 저장소는 일정 관리 앱의 화면과 사용자 흐름을 빠르게 검증하는 기획용 PoC입니다. 다음 순서로 판단합니다.

1. 아이디어를 빠르게 조작 가능한 화면으로 만든다.
2. 검토할 사용자 흐름을 명확하게 보여준다.
3. 코드를 쉽게 이해할 수 있게 유지한다.
4. 시각적 완성도와 확장성을 다듬는다.

## 기술 기준

- Vite + React + TypeScript
- 화면별 경로가 필요할 때 React Router
- CSS Modules 또는 단순 CSS
- 목 데이터와 필요한 경우 `localStorage`
- npm

정확한 패키지 버전과 설정은 최신 `main`의 `package.json`, `package-lock.json`, Vite·TypeScript·ESLint 설정을 단일 기준으로 삼습니다.

- 브랜치를 만들기 전에 최신 `main`을 반영하고 `npm ci`로 설치합니다.
- 핵심 패키지 버전, 패키지 매니저, 락파일, 설정을 기능 브랜치에서 임의로 바꾸지 않습니다.
- 기존 도구로 해결할 수 있는지 먼저 확인하고 같은 역할의 라이브러리를 중복 도입하지 않습니다.
- 새 패키지가 꼭 필요하면 npm으로 설치하고 두 패키지 파일을 함께 커밋하며 PR에 이유를 적습니다.
- 스택이나 버전 변경은 기능 개발과 분리된 PR로 진행합니다.

## 구현 원칙

- 화면 또는 사용자 흐름 하나를 작은 작업 단위로 삼습니다.
- 서버 동작은 목 데이터, 지연 함수, 브라우저 상태로 표현합니다.
- 검토에 중요한 로딩·빈 상태·오류 상태는 확인 가능하게 만듭니다.
- 기존 화면과 패턴을 우선 재사용하고, 필요가 확인되기 전에는 과도하게 추상화하지 않습니다.
- 접근 가능한 HTML과 기본 키보드 조작을 사용합니다.
- 요청과 무관한 리팩터링이나 파일 정리는 하지 않습니다.
- 명시적 요청 없이 백엔드, DB, 인증, 전역 상태 관리, 디자인 시스템, 배포 인프라를 만들지 않습니다.

## 작업 흐름

1. `README.md`, 이 문서, 파일 구조, `git status`를 확인합니다.
2. 최신 `main`에서 단일 화면은 `screen/`, 여러 단계의 사용자 흐름은 `flow/`, 아이디어 검증은 `experiment/` 브랜치로 시작합니다.
3. 화면을 구현하고 주요 상태와 상호작용을 직접 확인합니다.
4. `npm run lint`와 `npm run build`를 통과시킵니다.
5. 주요 변경 화면을 캡처합니다.
6. 변경사항을 커밋하고 원격 브랜치에 푸시합니다.
7. 목적, 구현 내용, 검토 포인트, 실행 방법, 화면 캡처가 포함된 PR을 생성합니다.

커밋 메시지는 `feat(screen): add weekly schedule prototype`처럼 작업 유형과 범위가 바로 드러나게 작성합니다.

## PR 형식

PR 본문은 `목적`, `구현 내용`, `검토 포인트`, `실행 방법`, `화면` 순서로 작성합니다. 화면 변경이 없는 경우 `화면`은 생략할 수 있습니다.

### 화면 캡처

화면이 달라지는 PR에는 실제 실행 화면을 1~3장 첨부합니다. 문서나 내부 코드만 바뀐 PR은 생략할 수 있습니다.

- 새 화면의 기본 상태를 포함하고, 필요하면 빈 상태·모달 등 핵심 상태를 추가합니다.
- PR 본문에 직접 업로드하고, 불가능하면 `docs/pr-screenshots/<작업-이름>/`에 저장해 커밋한 뒤 본문에 표시합니다.
- 캡처나 첨부에 실패하면 완료 처리하지 말고 원인과 로컬 파일 경로를 보고합니다.

## 완료 조건

- 요청한 화면과 목 데이터 기반 상호작용이 실행된다.
- lint와 build가 통과한다.
- 작업 브랜치가 커밋·푸시되어 있다.
- 화면 변경 시 캡처를 포함한 PR이 생성되어 있다.

푸시 또는 PR 생성이 권한·인증 문제로 불가능하면 성공한 단계와 막힌 원인을 명확히 보고합니다.
